---
phase: 01-core-analysis-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/cgm_insights/__init__.py
  - .python-version
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Project uses Python 3.10+ with all dependencies installed"
    - "Project follows src/ layout with installable package structure"
    - "Package can be imported successfully in Python"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package configuration with dependencies"
      contains: "polars"
      contains: "glucostats"
      contains: "pydantic"
    - path: "src/cgm_insights/__init__.py"
      provides: "Package initialization"
      min_lines: 1
  key_links:
    - from: "pyproject.toml"
      to: "Python 3.10+ runtime"
      via: "requires-python"
---

<objective>
Set up Python 3.10+ environment and create installable package structure with all dependencies.

Purpose: GlucoStats requires Python 3.10+. The project must be a properly structured Python package before any library code can be written.

Output: Working Python 3.10+ environment with Polars, GlucoStats, Pydantic, and openpyxl installed in a src/ layout package.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-core-analysis-library/01-RESEARCH.md

**Critical Blocker:** Current environment has Python 3.9.6. GlucoStats requires Python 3.10+ for modern type hints (`str | None` syntax). This MUST be resolved first.

**Sample Data Location:** `data/readings.csv` - CSV format with columns: date, datetime, time, mg_dl, trend

**Dependency Versions (from research):**
- Polars: 1.40.1
- GlucoStats: 1.0.0
- Pydantic: 2.13.3
- openpyxl: 3.1.3 (for future Excel support)
- python-dateutil: 2.9.0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Python and create pyproject.toml</name>
  <files>pyproject.toml, .python-version</files>
  <read_first>
    - This file does not exist yet (greenfield project)
  </read_first>
  <action>
    1. Install Python 3.10+ using available methods:
       - Check if `uv` is available: `uv python install 3.12`
       - If not, check for Homebrew: `brew install python@3.12`
       - If not, note that Python 3.10+ must be installed manually

    2. Create `.python-version` file with content: `3.12` (latest stable)

    3. Create `pyproject.toml` with the following exact content:

    ```toml
    [project]
    name = "cgm-insights"
    version = "0.1.0"
    description = "CGM data analysis library for glucose insights"
    requires-python = ">=3.10"
    dependencies = [
        "polars>=1.40.0",
        "glucostats>=1.0.0",
        "pydantic>=2.13.0",
        "openpyxl>=3.1.0",
        "python-dateutil>=2.9.0",
    ]

    [project.optional-dependencies]
    dev = [
        "pytest>=8.0",
        "ruff>=0.11.0",
    ]

    [build-system]
    requires = ["hatchling"]
    build-backend = "hatchling.build"

    [tool.hatch.build.targets.wheel]
    packages = ["src/cgm_insights"]

    [tool.ruff]
    line-length = 100
    target-version = "py310"

    [tool.pytest.ini_options]
    testpaths = ["tests"]
    python_files = ["test_*.py"]
    python_classes = ["Test*"]
    python_functions = ["test_*"]
    ```

    4. Create virtual environment and install dependencies:
       ```bash
       cd /Users/ffaber/claude-projects/sugarmate-reports
       python3.12 -m venv .venv 2>/dev/null || python3.10 -m venv .venv || python3 -m venv .venv
       source .venv/bin/activate
       pip install -e ".[dev]"
       ```

    5. Verify installation:
       ```bash
       python --version  # Must be 3.10+
       python -c "import polars; import pydantic; print('Dependencies OK')"
       python -c "import glucostats; print('GlucoStats OK')"  # This will fail on Python 3.9
       ```
  </action>
  <verify>
    <automated>python --version | grep -E "3\.(1[0-9]|[2-9][0-9])"</automated>
    <automated>test -f pyproject.toml && grep -q "polars" pyproject.toml && grep -q "glucostats" pyproject.toml</automated>
    <automated>python -c "import polars; import pydantic; import glucostats; print('All dependencies imported successfully')"</automated>
  </verify>
  <done>
    - Python version is 3.10 or higher
    - pyproject.toml exists with polars>=1.40.0, glucostats>=1.0.0, pydantic>=2.13.0
    - Virtual environment .venv exists
    - All dependencies can be imported successfully in Python
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src layout package structure</name>
  <files>src/cgm_insights/__init__.py, src/cgm_insights/models/__init__.py, src/cgm_insights/ingestion/__init__.py, src/cgm_insights/analytics/__init__.py, src/cgm_insights/output/__init__.py, tests/__init__.py</files>
  <read_first>
    - This is a greenfield project; no existing structure to reference
  </read_first>
  <action>
    Create the following directory structure with `__init__.py` files:

    ```
    src/
    └── cgm_insights/
        ├── __init__.py          # Public API exports (initially empty __all__)
        ├── models/
        │   └── __init__.py      # Model exports
        ├── ingestion/
        │   └── __init__.py      # Parser and validator exports
        ├── analytics/
        │   └── __init__.py      # Metrics exports
        └── output/
            └── __init__.py      # Formatter exports
    tests/
    └── __init__.py              # Test package
    ```

    Create each `__init__.py` with minimal content:

    - `src/cgm_insights/__init__.py`:
      ```python
      """CGM Insights - Core analysis library for glucose data."""

      __version__ = "0.1.0"
      __all__ = ["__version__"]
      ```

    - Submodule `__init__.py` files: Empty files (will be populated in later plans)

    Verify the package is importable:
    ```bash
    python -c "from cgm_insights import __version__; print(f'Package version: {__version__}')"
    ```
  </action>
  <verify>
    <automated>test -f src/cgm_insights/__init__.py</automated>
    <automated>test -f src/cgm_insights/models/__init__.py</automated>
    <automated>test -f src/cgm_insights/ingestion/__init__.py</automated>
    <automated>test -f src/cgm_insights/analytics/__init__.py</automated>
    <automated>test -f src/cgm_insights/output/__init__.py</automated>
    <automated>python -c "from cgm_insights import __version__; print(__version__)"</automated>
  </verify>
  <done>
    - src/cgm_insights/ package structure exists
    - All submodules have __init__.py files
    - Package can be imported: `from cgm_insights import __version__` succeeds
    - Version 0.1.0 is accessible
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → Parser | Untrusted file input from user uploads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | File upload | mitigate | Parser will validate file extension and size limits (future plan) |
| T-01-02 | Tampering | Glucose values | mitigate | Pydantic models will validate physiological ranges 40-400 mg/dL (Plan 02) |
| T-01-03 | Repudiation | Medical claims | mitigate | All user-facing text uses wellness language; disclaimer required (throughout) |
</threat_model>

<verification>
- [ ] Python version is 3.10 or higher
- [ ] pyproject.toml exists with correct dependencies
- [ ] Virtual environment created and activated
- [ ] All dependencies installed successfully
- [ ] Package structure follows src/ layout
- [ ] Package is importable in Python
</verification>

<success_criteria>
- Python 3.10+ runtime environment active
- All core dependencies (polars, glucostats, pydantic) importable
- Package structure follows src/cgm_insights layout
- Package can be imported: `from cgm_insights import __version__`
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-analysis-library/01-01-SUMMARY.md`
</output>