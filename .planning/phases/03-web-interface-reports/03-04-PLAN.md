---
phase: 03-web-interface-reports
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified: []
autonomous: true
requirements: [RPT-01, RPT-02]
user_setup: []

must_haves:
  truths:
    - "Upload endpoint handles valid and invalid files correctly"
    - "Results page displays all metrics from analysis"
    - "AGP export generates valid PDF with all required content"
    - "Error handling returns appropriate HTTP status codes"
  artifacts:
    - path: "tests/web/__init__.py"
      provides: "Web module tests"
    - path: "tests/web/test_upload.py"
      provides: "Upload endpoint tests"
    - path: "tests/web/test_results.py"
      provides: "Results endpoint tests"
    - path: "tests/web/test_export.py"
      provides: "AGP export tests"
    - path: "tests/web/test_session.py"
      provides: "Session management tests"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
  key_links:
    - from: "tests/web/test_upload.py"
      to: "src/web/routes/upload.py"
      via: "FastAPI TestClient"
      pattern: "client.post('/upload'"
    - from: "tests/web/test_results.py"
      to: "src/web/routes/results.py"
      via: "FastAPI TestClient"
      pattern: "client.get('/results/{session_id}'"
    - from: "tests/web/test_export.py"
      to: "src/web/routes/export.py"
      via: "FastAPI TestClient"
      pattern: "client.get('/export/{session_id}/agp'"
---

<objective>
Create comprehensive tests for web interface endpoints and AGP report generation.

Purpose: Ensure web endpoints handle all cases correctly, AGP reports contain required content, and integration with core library works.
Output: Test suite covering upload, results, export, and session management with fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Test fixtures and utilities needed -->

From tests/conftest.py (to be created):
- sample_readings: list[CGMReading] - Valid test readings
- sample_results: AnalysisResults - Pre-computed test results
- sample_session_id: str - Test session ID
- test_client: TestClient - FastAPI test client fixture
- temp_csv_file: Path - Temporary CSV file for upload testing

From src/web/app.py:
- app: FastAPI instance for testing

Test structure follows existing tests/ directory pattern from Phase 1 & 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures and shared utilities</name>
  <files>tests/conftest.py, tests/web/__init__.py, tests/fixtures/sample_data.py</files>
  <read_first>tests/conftest.py (if exists), src/web/app.py, tests/fixtures/ (any existing)</read_first>
  <action>Create shared test fixtures for web testing.

1. Create tests/web/__init__.py (empty, marks package)

2. Update tests/conftest.py with web fixtures:
   - test_client fixture using FastAPI TestClient
   - sample_csv_content fixture - valid CSV content for upload
   - sample_session fixture - creates session with sample results
   - temp_upload_dir fixture - temporary directory for file uploads

3. Create tests/fixtures/sample_data.py with:
   - generate_sample_readings(count: int, days: int) - Creates realistic CGM readings
   - create_sample_csv(content: str) -> Path - Creates temp CSV file
   - SAMPLE_RESULTS_DICT - Pre-computed results dictionary for testing

Test fixtures should use existing cgm_insights test patterns from Phase 1 & 2.</action>
  <verify>
    <automated>uv run pytest tests/conftest.py --collect-only 2>&1 | head -20</automated>
  </verify>
  <done>Test fixtures exist and can be imported by web tests</done>
</task>

<task type="auto">
  <name>Task 2: Test upload endpoint and file handling</name>
  <files>tests/web/test_upload.py</files>
  <read_first>src/web/routes/upload.py, tests/conftest.py</read_first>
  <action>Create tests for upload endpoint with various scenarios.

Test cases for tests/web/test_upload.py:

1. Test successful upload:
   - POST /upload with valid CSV file
   - Assert 200 response
   - Assert session_id in response
   - Assert session stored with results

2. Test invalid file type:
   - POST /upload with .txt file
   - Assert 400 response
   - Assert error message about file type

3. Test empty file:
   - POST /upload with empty file
   - Assert 400 response

4. Test insufficient data:
   - POST /upload with CSV with too few readings
   - Assert 422 response

5. Test file size limit:
   - POST /upload with large file (>10MB)
   - Assert 413 response (payload too large)

6. Test date range parameters:
   - POST /upload with start_date and end_date
   - Assert results reflect date filtering

7. Test session persistence:
   - Upload file, get session_id
   - Verify session_id retrieves same results

Use pytest-asyncio for async endpoint testing if needed.</action>
  <verify>
    <automated>uv run pytest tests/web/test_upload.py -v --tb=short</automated>
  </verify>
  <done>All upload endpoint tests pass with >=95% coverage of upload.py</done>
</task>

<task type="auto">
  <name>Task 3: Test results display and session management</name>
  <files>tests/web/test_results.py, tests/web/test_session.py</files>
  <read_first>src/web/routes/results.py, src/web/services/session.py</read_first>
  <action>Create tests for results endpoint and session management.

Test cases for tests/web/test_session.py:

1. Test session creation:
   - store_session() creates unique session_id
   - session_id is valid UUID format
   - get_session() returns stored data

2. Test session retrieval:
   - get_session_results() returns dict
   - get_session_patterns() returns list
   - Invalid session_id returns None

3. Test session isolation:
   - Multiple sessions stored separately
   - Overwriting session_id replaces data

Test cases for tests/web/test_results.py:

1. Test successful results display:
   - GET /results/{session_id} with valid session
   - Assert 200 response
   - Assert template rendered with correct metrics

2. Test invalid session:
   - GET /results/{session_id} with invalid UUID
   - Assert 404 response

3. Test results contain all metrics:
   - Assert response contains average_glucose
   - Assert response contains time_in_range breakdown
   - Assert response contains patterns
   - Assert response contains suggestions

4. Test wellness disclaimer:
   - Assert disclaimer present in response</action>
  <verify>
    <automated>uv run pytest tests/web/test_results.py tests/web/test_session.py -v --tb=short</automated>
  </verify>
  <done>All results and session tests pass with >=95% coverage</done>
</task>

<task type="auto">
  <name>Task 4: Test AGP export and PDF generation</name>
  <files>tests/web/test_export.py, tests/web/test_agp_generator.py</files>
  <read_first>src/web/routes/export.py, src/web/services/agp_generator.py</read_first>
  <action>Create tests for AGP export endpoint and PDF generation.

Test cases for tests/web/test_agp_generator.py:

1. Test PDF generation:
   - generate_agp_report() returns bytes
   - PDF starts with %PDF header
   - PDF contains expected sections

2. Test PDF content:
   - PDF contains date range
   - PDF contains glucose metrics
   - PDF contains TIR breakdown
   - PDF contains wellness disclaimer

3. Test PDF with empty results:
   - Handles edge case gracefully
   - Returns valid PDF with "No data" message

Test cases for tests/web/test_export.py:

1. Test successful export:
   - GET /export/{session_id}/agp with valid session
   - Assert 200 response
   - Assert Content-Type: application/pdf
   - Assert Content-Disposition has filename

2. Test invalid session:
   - GET /export/{session_id}/agp with invalid session_id
   - Assert 404 response

3. Test PDF download headers:
   - Assert Content-Disposition: attachment
   - Assert filename format: agp-report-{date}.pdf</action>
  <verify>
    <automated>uv run pytest tests/web/test_export.py tests/web/test_agp_generator.py -v --tb=short</automated>
  </verify>
  <done>All export and AGP generator tests pass with >=95% coverage</done>
</task>

<task type="auto">
  <name>Task 5: Integration test and coverage verification</name>
  <files>tests/web/test_integration.py</files>
  <read_first>tests/web/test_upload.py, tests/web/test_results.py, tests/web/test_export.py</read_first>
  <action>Create end-to-end integration test and verify coverage.

1. Create tests/web/test_integration.py with:

   Test full workflow:
   - Upload file -> get session_id
   - Retrieve results with session_id
   - Export AGP report
   - Verify all steps work together

2. Run full test suite:
   - uv run pytest tests/ -v
   - Verify all tests pass

3. Generate coverage report:
   - uv run pytest tests/ --cov=src/web --cov-report=term-missing
   - Verify >=90% coverage on web module

4. Create test summary in SUMMARY.md:
   - List all test cases
   - Note coverage percentage
   - Document any edge cases tested

Integration test validates the full user journey from upload to export.</action>
  <verify>
    <automated>uv run pytest tests/ -v --cov=src/web --cov-report=term-missing 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass, web module coverage >=90%, integration test validates full workflow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> Code | Tests verify behavior at boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | Testing | Test isolation | mitigate | Use fixtures and temp directories for isolation |
</threat_model>

<verification>
- All web tests pass
- Coverage report shows >=90% on web module
- Integration test validates full workflow
- Test suite runs in under 60 seconds
</verification>

<success_criteria>
- Upload endpoint handles all error cases correctly
- Results page displays all metrics and patterns
- AGP export generates valid PDF with required content
- Session management isolates user data
- Full test suite passes with high coverage
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-interface-reports/03-04-SUMMARY.md`
</output>