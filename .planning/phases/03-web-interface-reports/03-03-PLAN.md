---
phase: 03-web-interface-reports
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: true
requirements: [RPT-01, RPT-02]
user_setup: []

must_haves:
  truths:
    - "User can export AGP (Ambulatory Glucose Profile) report for healthcare sharing"
    - "AGP report includes glucose profile with all standard metrics"
    - "AGP report includes daily glucose visualization"
    - "AGP report includes data statistics and quality information"
  artifacts:
    - path: "src/web/routes/export.py"
      provides: "AGP report export endpoint"
      exports: ["router"]
    - path: "src/web/services/agp_generator.py"
      provides: "AGP report PDF generation"
      exports: ["generate_agp_report"]
    - path: "src/web/templates/agp_report.html"
      provides: "AGP report HTML template"
    - path: "src/web/templates/agp_print.css"
      provides: "Print-specific CSS for AGP report"
  key_links:
    - from: "src/web/routes/export.py"
      to: "src/web/services/agp_generator.py"
      via: "generate_agp_report(session_id, results)"
      pattern: "generate_agp_report"
    - from: "src/web/services/agp_generator.py"
      to: "weasyprint or reportlab"
      via: "PDF generation library"
      pattern: "HTML_to_PDF"
    - from: "src/web/services/agp_generator.py"
      to: "cgm_insights.format_results"
      via: "import and call"
      pattern: "from cgm_insights import format_results"
---

<objective>
Create AGP (Ambulatory Glucose Profile) report generation for healthcare provider sharing.

Purpose: Enable users to export standardized AGP reports containing all glucose metrics, visualizations, and quality information for healthcare discussions.
Output: PDF export endpoint generating professional AGP reports following standard format.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-interface-reports/03-CONTEXT.md

<interfaces>
<!-- AGP report data requirements from core library -->

From src/cgm_insights/models/results.py:
```python
class AnalysisResults(BaseModel):
    date_range_start: datetime
    date_range_end: datetime
    total_readings: int
    time_in_range: TimeInRange  # All 5 bands
    average_glucose: float  # mg/dL
    glucose_std: float
    cv_pct: float
    gmi: float
    completeness_pct: float
```

From src/cgm_insights/analytics/patterns.py:
```python
class PatternResult(BaseModel):
    pattern_type: PatternType
    description: str
    severity: PatternSeverity
    avg_glucose: float
    reading_count: int
```

Standard AGP Report Elements (RPT-02):
1. Glucose Profile: Average, GMI, CV, Time-in-Range breakdown
2. Daily Glucose: Visual representation (ambulatory glucose profile)
3. Data Statistics: Reading count, completeness, date range, quality flags
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AGP report HTML template</name>
  <files>src/web/templates/agp_report.html, src/web/templates/agp_print.css</files>
  <read_first>src/web/templates/base.html, src/cgm_insights/models/results.py, src/cgm_insights/output/formatter.py</read_first>
  <action>Create AGP report template following standard format.

1. Create src/web/templates/agp_report.html:
   - Professional print-ready HTML structure
   - Header with title "Ambulatory Glucose Profile (AGP) Report"
   - Generated date and user identifier section (placeholder for name)
   - Analysis period dates

   Section 1 - Glucose Profile:
   - Average Glucose (mg/dL)
   - Glucose Management Indicator (GMI) with caveat
   - Coefficient of Variation (CV%)
   - Time in Range breakdown table:
     - Very Low (<54 mg/dL): X%
     - Low (54-70 mg/dL): X%
     - Target (70-180 mg/dL): X%
     - High (180-250 mg/dL): X%
     - Very High (>250 mg/dL): X%

   Section 2 - Daily Glucose Pattern:
   - Visual chart placeholder (rendered in PDF)
   - Time-of-day patterns summary

   Section 3 - Data Statistics:
   - Total readings count
   - Completeness percentage
   - Analysis date range
   - Data quality flags (if any)

   Section 4 - Notes for Healthcare Provider:
   - Wellness disclaimer
   - Space for handwritten notes

   Footer:
   - "Generated by CGM Insights" with timestamp
   - "This report is for informational purposes only"

2. Create src/web/templates/agp_print.css:
   - @media print rules for clean printing
   - Page break handling
   - Chart rendering for print
   - Font sizing for print clarity
   - Remove navigation and non-essential elements

Template receives: session_id, results (dict), patterns (list), generated_date (datetime)</action>
  <verify>
    <automated>ls -la src/web/templates/agp_report.html src/web/templates/agp_print.css</automated>
  </verify>
  <done>AGP report template exists with all standard AGP sections and print-optimized CSS</done>
</task>

<task type="auto">
  <name>Task 2: Create AGP PDF generation service</name>
  <files>src/web/services/agp_generator.py</files>
  <read_first>src/web/services/session.py, src/cgm_insights/output/formatter.py, src/web/templates/agp_report.html</read_first>
  <action>Create PDF generation service using HTML-to-PDF approach.

1. Add weasyprint dependency to pyproject.toml (for HTML-to-PDF conversion)

2. Create src/web/services/agp_generator.py with:
   - generate_agp_report(session_id: str, results: AnalysisResults, patterns: list) -> bytes
   - Render agp_report.html template with Jinja2
   - Generate glucose trend chart as embedded image (using matplotlib or chart in HTML)
   - Convert HTML to PDF using weasyprint
   - Return PDF bytes for download

3. Include in AGP PDF:
   - All metrics from format_results()
   - Glucose trend visualization (simplified line chart)
   - Time-of-day pattern summary (text-based)
   - Data quality information
   - Generated timestamp

4. PDF formatting:
   - Portrait orientation
   - A4 or Letter page size
   - Margins for printing
   - Embedded fonts (optional)

The PDF should be suitable for printing and sharing with healthcare providers. Use weasyprint's HTML-to-PDF for consistent rendering.</action>
  <verify>
    <automated>uv run python -c "from web.services.agp_generator import generate_agp_report; print('AGP generator loaded')"</automated>
  </verify>
  <done>AGP generator creates PDF bytes from analysis results with all standard elements</done>
</task>

<task type="auto">
  <name>Task 3: Create export route and download endpoint</name>
  <files>src/web/routes/export.py, src/web/app.py</files>
  <read_first>src/web/routes/results.py, src/web/services/agp_generator.py</read_first>
  <action>Create export endpoint for AGP report download.

1. Create src/web/routes/export.py with:
   - GET /export/{session_id}/agp endpoint
   - Retrieve results from session store
   - Handle missing session with 404
   - Call generate_agp_report() to create PDF
   - Return PDF as StreamingResponse with:
     - Content-Type: application/pdf
     - Content-Disposition: attachment; filename="agp-report-{date}.pdf"
   - Error handling for PDF generation failures

2. Update src/web/app.py to include export router

3. Add download button to results.html:
   - Button links to /export/{session_id}/agp
   - DaisyUI button styling
   - HTMX for download progress indication (optional)

4. Add GET /export/{session_id}/preview endpoint (optional):
   - Returns HTML preview of AGP report
   - Useful for checking before download

Export flow:
1. User clicks "Export AGP Report" button
2. Browser requests GET /export/{session_id}/agp
3. Server retrieves session results
4. Server generates PDF using agp_generator
5. Server returns PDF as download</action>
  <verify>
    <automated>uv run python -c "from web.routes.export import router; print(f'Export routes: {len(router.routes)}')"</automated>
  </verify>
  <done>Export endpoint returns PDF download with correct headers and AGP content</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session ID -> PDF | Session lookup before PDF generation |
| PDF content -> User | Generated content must be safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | PDF generation | mitigate | Timeout PDF generation, limit PDF size |
| T-03-08 | Information Disclosure | Session enumeration | mitigate | Rate limit export endpoint, use UUID v4 session IDs |
</threat_model>

<verification>
- Export route registered in app
- PDF generation produces valid PDF bytes
- PDF download returns correct Content-Type and filename
- PDF contains all AGP standard elements
- PDF prints correctly (test with print preview)
</verification>

<success_criteria>
- User can click "Export AGP Report" button on results page
- Browser downloads PDF file with correct filename
- PDF contains: glucose profile metrics, TIR breakdown, daily pattern summary, data statistics
- PDF includes wellness disclaimer
- PDF is suitable for printing and healthcare sharing
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-interface-reports/03-03-SUMMARY.md`
</output>