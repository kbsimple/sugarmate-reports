---
phase: 08-tod-chart-patterns-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/templates/results.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "Time-of-Day chart renders data (not blank) for any uploaded file with time-of-day patterns"
    - "The scripts block in results.html declares const patterns before charts.js is loaded"
    - "All existing template-rendering tests continue to pass"
  artifacts:
    - path: "src/web/templates/results.html"
      provides: "JS const injection for patterns variable"
      contains: "const patterns = {{ patterns | tojson }};"
  key_links:
    - from: "src/web/templates/results.html"
      to: "src/web/static/js/charts.js"
      via: "const patterns declaration before charts.js script tag"
      pattern: "const patterns = .*tojson"
---

<objective>
Fix the blank Time-of-Day chart by injecting the missing `const patterns` JS variable into the
results.html scripts block. The chart canvas exists and charts.js is correct — the single missing
line is preventing the guard `typeof patterns !== 'undefined'` in charts.js from passing.

Purpose: Users who have uploaded CGM data see a blank chart where they should see their time-of-day
glucose patterns. One missing line of template code is the root cause.

Output: `results.html` with `const patterns = {{ patterns | tojson }};` added to the scripts block.
Time-of-Day chart renders on page load. All existing tests pass.
</objective>

<execution_context>
@/Users/ffaber/claude-projects/sugarmate-reports/.planning/phases/08-tod-chart-patterns-ux/08-RESEARCH.md
@/Users/ffaber/claude-projects/sugarmate-reports/.planning/phases/08-tod-chart-patterns-ux/08-PATTERNS.md
</execution_context>

<context>
@/Users/ffaber/claude-projects/sugarmate-reports/.planning/ROADMAP.md
@/Users/ffaber/claude-projects/sugarmate-reports/.planning/STATE.md

<interfaces>
<!-- From src/web/static/js/charts.js (verified by research) -->
<!-- The guard that blocks chart rendering when patterns is undeclared: -->
```javascript
// charts.js guard (line ~327):
if (typeof patterns !== 'undefined' && patterns && patterns.length > 0) {
    createDailyPatternsChart('dailyPatternsChart', patterns);
}
```
<!-- createDailyPatternsChart filters internally: p.type === 'time_of_day' -->

<!-- From src/web/routes/results.py (verified by research, line 131): -->
<!-- The template context key is exactly "patterns" and its value is formatted_patterns -->
<!-- (a list of PatternResult dicts with keys: type, time_period, avg_glucose, -->
<!--  reading_count, severity, description) -->
<!-- This is already passed via {% with patterns=patterns %} at results.html line 124 -->
<!-- AND is already in the outer template scope -->

<!-- CRITICAL: "patterns" is NOT behavioral_patterns. It is the PatternResult list -->
<!-- from patterns.py (time_of_day and day_of_week patterns). -->
<!-- behavioral_patterns is a separate template variable for the Phase 4 component. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject const patterns into results.html scripts block</name>
  <files>src/web/templates/results.html</files>

  <read_first>
    - src/web/templates/results.html — read the ENTIRE file; specifically verify the current scripts
      block at lines 195–202 before making any edit; confirm it currently reads exactly:
        const tirData = {{ tir_data | tojson }};
        const glucoseReadings = {{ glucose_readings | tojson }};
      with NO const patterns line present
    - src/web/static/js/charts.js — read lines 310–340 to confirm the typeof patterns guard exists
      and that no other currently-broken chart functions exist
  </read_first>

  <action>
    Edit src/web/templates/results.html.

    TARGET: The {% block scripts %} block at the bottom of the file (lines 195–202).

    CURRENT state (broken):
    ```html
    {% block scripts %}
    <script>
    // Chart data from template
    const tirData = {{ tir_data | tojson }};
    const glucoseReadings = {{ glucose_readings | tojson }};
    </script>
    <script src="/static/js/charts.js"></script>
    {% endblock %}
    ```

    REPLACEMENT (fixed):
    ```html
    {% block scripts %}
    <script>
    // Chart data from template
    const tirData = {{ tir_data | tojson }};
    const glucoseReadings = {{ glucose_readings | tojson }};
    const patterns = {{ patterns | tojson }};
    </script>
    <script src="/static/js/charts.js"></script>
    {% endblock %}
    ```

    The ONLY change is adding `const patterns = {{ patterns | tojson }};` as the third line
    inside the <script> tag, after the glucoseReadings line and before the closing </script>.

    DO NOT change anything else in the file.

    CRITICAL variable name warning: use exactly `patterns` (not `behavioral_patterns`, not
    `formatted_patterns`). The charts.js guard checks `typeof patterns !== 'undefined'` using
    exactly this name.

    PITFALL: patterns may be an empty list [] — this is fine. charts.js already handles it:
    `if (... patterns.length > 0)` exits cleanly. No special empty-list handling needed.
  </action>

  <verify>
    Run the full template test suite and check that the const line is present:

    ```bash
    cd /Users/ffaber/claude-projects/sugarmate-reports && \
    grep -n "const patterns" src/web/templates/results.html && \
    python -m pytest tests/web/test_results.py::TestResultsTemplateRendering -v
    ```

    Expected grep output:
      `199:const patterns = {{ patterns | tojson }};`
      (line number may differ by 1 if prior context shifted)

    Expected pytest output: all 4 tests in TestResultsTemplateRendering pass (PASSED).
  </verify>

  <acceptance_criteria>
    - `grep "const patterns = {{ patterns | tojson }};" src/web/templates/results.html` exits 0
      and prints a match
    - `grep "const patterns" src/web/templates/results.html | wc -l` prints 1 (exactly one
      occurrence — no duplicate declaration)
    - The const patterns line appears BEFORE `<script src="/static/js/charts.js">` in the file
      (grep line number for const patterns < grep line number for charts.js src)
    - `python -m pytest tests/web/test_results.py::TestResultsTemplateRendering -v` exits 0 with
      4 passed tests
    - `python -m pytest tests/web/test_results.py -v` exits 0 with all tests passing
    - No new test failures introduced in the full test suite:
      `python -m pytest --tb=short -q 2>&1 | tail -5` shows no FAILED lines
  </acceptance_criteria>

  <done>
    results.html scripts block declares `const patterns = {{ patterns | tojson }};` between the
    glucoseReadings const and the charts.js script tag. The charts.js `typeof patterns` guard will
    pass, calling createDailyPatternsChart when time-of-day patterns are present. All template
    rendering tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Browser | Jinja2 renders session data into JS constants; the `tojson` filter is the trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | `{{ patterns \| tojson }}` injection | accept | `tojson` escapes all HTML-special characters; `formatted_patterns` contains no PII (only statistical aggregates: avg_glucose, time_period, severity). Starlette's Jinja2 `tojson` filter applies JSON encoding with HTML-safe escaping by default. No additional mitigation needed. |
| T-08-01-02 | Tampering | JS variable accessible to page JS | accept | The injected data is already visible through the /data JSON endpoint; injecting it into the page adds no new attack surface. The user is the sole consumer of their own session data. |
</threat_model>

<verification>
After plan execution:
1. `grep "const patterns = {{ patterns | tojson }};" src/web/templates/results.html` must match
2. `python -m pytest tests/web/test_results.py -v` must exit 0
3. `python -m pytest --tb=short -q` full suite must exit 0
</verification>

<success_criteria>
- Time-of-Day chart JavaScript guard can pass: `typeof patterns !== 'undefined'` evaluates to
  true after the fix
- results.html scripts block has exactly 3 const declarations: tirData, glucoseReadings, patterns
- All 240 existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-tod-chart-patterns-ux/08-01-SUMMARY.md`
</output>
