---
phase: 02-cli-tool-insights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/cgm_insights/cli.py, tests/test_cli/__init__.py, tests/test_cli/test_cli.py]
autonomous: true
requirements: [VIZ-02]

must_haves:
  truths:
    - "User can run cgm-insights analyze <file> from terminal"
    - "User can specify date range with --start and --end arguments"
    - "User sees glucose metrics and time-in-range in formatted output"
    - "User sees data quality warnings if issues detected"
  artifacts:
    - path: "src/cgm_insights/cli.py"
      provides: "CLI entry point with analyze command"
      min_lines: 50
    - path: "tests/test_cli/test_cli.py"
      provides: "CLI tests for analyze command"
      contains: "def test_analyze_command"
  key_links:
    - from: "src/cgm_insights/cli.py"
      to: "cgm_insights.analyze_file"
      via: "direct import and call"
      pattern: "from cgm_insights import analyze_file"
    - from: "src/cgm_insights/cli.py"
      to: "cgm_insights.format_summary"
      via: "direct import and call"
      pattern: "from cgm_insights import format_summary"
---

<objective>
Create CLI entry point using Typer framework with analyze command that accepts file path and date range arguments, calls the core analysis library, and displays results as formatted text output.

Purpose: Enable users to run CGM analysis from terminal as foundation for visualization and insights integration.
Output: Working `cgm-insights analyze <file>` command with basic text output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 1 Public API (existing)
From src/cgm_insights/__init__.py:
```python
__all__ = [
    "__version__",
    "CGMReading",
    "ValidationResult",
    "AnalysisResults",
    "TimeInRange",
    "SugarmateParser",
    "get_parser",
    "validate_completeness",
    "filter_by_date_range",
    "exclude_warmup_period",
    "normalize_for_glucostats",
    "calculate_metrics",
    "check_minimum_data",
    "GMI_CAVEAT",
    "QUALITY_WARNING",
    "format_results",
    "format_quality_flags",
    "format_summary",
]

def analyze_file(
    file_path: str,
    start_date: str | None = None,
    end_date: str | None = None,
    exclude_warmup: bool = True,
) -> AnalysisResults:
    ...
```

# Tech Stack
- Typer 0.24.2 for CLI (already in dependencies per STACK.md)
- Rich for terminal output (comes with Typer)

# Existing format_summary output format:
```
Analysis Period: {start} to {end}
Readings: {count} ({completeness}% complete)

Glucose Metrics:
  Average: {avg} mg/dL
  Std Dev: {std} mg/dL
  CV: {cv}%
  GMI: {gmi}%

Time in Range:
  Very Low (<54): {pct}%
  Low (54-70): {pct}%
  Target (70-180): {pct}%
  High (180-250): {pct}%
  Very High (>250): {pct}%

Quality Flags:
  - {flag}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI module with Typer analyze command</name>
  <files>src/cgm_insights/cli.py</files>
  <read_first>
    - src/cgm_insights/__init__.py (see public API exports)
    - src/cgm_insights/output/formatter.py (see format_summary function)
    - pyproject.toml (see current dependencies)
  </read_first>
  <action>
    Create src/cgm_insights/cli.py with:
    1. Import typer and create app = typer.Typer()
    2. Define analyze() command with parameters:
       - file_path: Path (positional, required) - path to CGM data file
       - start_date: Optional[str] = None (option --start)
       - end_date: Optional[str] = None (option --end)
       - exclude_warmup: bool = True (option --exclude-warmup/--include-warmup)
    3. Command implementation:
       - Call analyze_file(file_path, start_date, end_date, exclude_warmup)
       - Use format_summary(results) for text output
       - Print to console using typer.echo()
       - Handle ValueError exceptions with typer.Exit(1) and error message
    4. Add help text: "Analyze CGM data from file and display metrics."
    5. Create main() function that calls app()
    6. Add if __name__ == "__main__": main() for direct execution
    7. Import and use format_quality_flags for quality warnings if flags present
    8. Print GMI_CAVEAT disclaimer at end of output

    DO NOT add visualization yet - this plan is for basic text output.
    DO NOT add pattern detection yet - that comes in later plans.

    Follow Typer conventions: use typer.Argument for positional, typer.Option for flags.
  </action>
  <verify>
    <automated>python -c "from cgm_insights.cli import app; print('CLI importable')" && echo "PASS"</automated>
    <automated>python -m cgm_insights.cli --help | grep -q "analyze" && echo "PASS"</automated>
  </verify>
  <done>
    - CLI module imports without errors
    - `cgm-insights --help` shows available commands
    - `cgm-insights analyze --help` shows command options
    - analyze command accepts file_path, --start, --end, --exclude-warmup options
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CLI tests for analyze command</name>
  <files>tests/test_cli/__init__.py, tests/test_cli/test_cli.py</files>
  <read_first>
    - src/cgm_insights/cli.py (the module being tested)
    - tests/test_integration/test_analyze_file.py (see test patterns from Phase 1)
    - tests/test_output/test_formatter.py (see how format functions are tested)
  </read_first>
  <behavior>
    - Test 1: analyze command exists and accepts file path argument
    - Test 2: analyze command accepts --start and --end date options
    - Test 3: analyze command returns error for missing file
    - Test 4: analyze command returns error for invalid file format
  </behavior>
  <action>
    Create tests/test_cli/__init__.py (empty file).

    Create tests/test_cli/test_cli.py with:
    1. Import typer.testing.CliRunner for testing Typer apps
    2. Create runner = CliRunner() fixture pattern
    3. Test test_analyze_command_basic():
       - Use sample CSV file path from tests/test_ingestion/fixtures/ or create minimal test file
       - Run runner.invoke(app, ["analyze", test_file])
       - Assert exit_code == 0
       - Assert output contains "Analysis Period" and "Time in Range"
    4. Test test_analyze_command_with_dates():
       - Run runner.invoke(app, ["analyze", test_file, "--start", "2024-01-01", "--end", "2024-01-07"])
       - Assert exit_code == 0
       - Assert command accepts date arguments
    5. Test test_analyze_command_missing_file():
       - Run runner.invoke(app, ["analyze", "/nonexistent/file.csv"])
       - Assert exit_code == 1 (error)
       - Assert error message mentions file not found or cannot parse
    6. Test test_analyze_command_help():
       - Run runner.invoke(app, ["analyze", "--help"])
       - Assert exit_code == 0
       - Assert output contains "--start" and "--end" options

    Use tempfile or fixtures for test data files. Follow patterns from test_analyze_file.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli/ -v --tb=short 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -10</automated>
  </verify>
  <done>
    - All CLI tests pass
    - Test coverage for analyze command with and without options
    - Tests verify error handling for invalid inputs
  </done>
</task>

<task type="auto">
  <name>Task 3: Register CLI as console script entry point</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current project configuration)
    - src/cgm_insights/cli.py (the entry point function)
  </read_first>
  <action>
    Update pyproject.toml to add console script entry point:
    1. Add [project.scripts] section after dependencies
    2. Add: cgm-insights = "cgm_insights.cli:app"
    3. This makes the CLI available as `cgm-insights` command after pip install

    Verify the cli.py has the correct structure:
    - app = typer.Typer() must exist
    - @app.command() decorator on analyze function
    - if __name__ == "__main__": app() for direct execution

    After updating pyproject.toml, reinstall with: uv pip install -e .
  </action>
  <verify>
    <automated>uv pip install -e . 2>&1 && cgm-insights --help | grep -q "analyze" && echo "PASS"</automated>
  </verify>
  <done>
    - pyproject.toml has [project.scripts] section with cgm-insights entry
    - Running `cgm-insights --help` works from command line
    - Running `cgm-insights analyze --help` shows command options
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input → Core library | Untrusted file paths and date strings from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | File path input | mitigate | Validate file exists before processing; use pathlib.Path for safe path handling |
| T-02-02 | Denial of Service | Large file processing | accept | CLI processes local files; user controls input size; acceptable risk |
| T-02-03 | Information Disclosure | Error messages | mitigate | Catch exceptions and show user-friendly messages; do not expose internal paths in errors |
</threat_model>

<verification>
- [ ] `cgm-insights --help` displays available commands
- [ ] `cgm-insights analyze --help` shows --start, --end, --exclude-warmup options
- [ ] `cgm-insights analyze <file>` produces formatted output with metrics
- [ ] CLI handles missing files with clear error message
- [ ] CLI handles invalid date formats with clear error message
- [ ] All tests pass: `python -m pytest tests/test_cli/`
</verification>

<success_criteria>
- CLI entry point registered and working from command line
- analyze command accepts file path and date range arguments
- Output displays glucose metrics and time-in-range using format_summary
- Error handling provides user-friendly messages
- All CLI tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-tool-insights/02-01-SUMMARY.md`
</output>